#include <stdio.h>
#include <string.h>

int main()
{
  char firstName[15];

  while(1)
  {
    printf("Enter a name\n>");
    scanf("%s", firstName);

    for(int i = 0; i < strlen(firstName); i++)
      {
	printf("%c\n", firstName[i]);
      }
  }

  return 0;
}
